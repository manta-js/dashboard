// Import the implementation directly so an override wrapper can consume the
// original Shell without traversing the overridden barrel again.
export { Shell } from "../components/layout/shell/shell"
export type { ShellProps } from "../components/layout/shell/types"
